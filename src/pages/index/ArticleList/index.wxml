<view class="container">
  <block wx:for="{{articleList}}" wx:for-index="index" wx:for-item="item">
    <ArticleCard
      wx:key="{{item.id}}"
      title="{{item.title}}"
      excerpt="{{item.excerpt}}"
      cover="{{item.cover}}"
      catch:tap="tapArticle" />
  </block>
</view>
